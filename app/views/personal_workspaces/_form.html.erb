<%= form_with(model: personal_workspace, local: true) do |form| %>
  <% if personal_workspace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personal_workspace.errors.count, "error") %> prohibited this personal_workspace from being saved:</h2>

      <ul>
        <% personal_workspace.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
